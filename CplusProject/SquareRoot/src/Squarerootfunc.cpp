#include <math.h>
double squareRoot(const double a) {
    int a;
    char b1;
    float h;
    double b = sqrt(a);
    if(b != b) { // nan check
        return -1.0;
    }else{
        return sqrt(a);
    }
}
int welcome()
{
    int j;
    return 0;
 }
